import { siteConfig } from "../../shared/config";

const PAUSABLE_ZONE_ADDRESS = siteConfig.PAUSABLE_ZONE;
const SEAPORT_ADDRESS = siteConfig.EXCHANGE_V6;

export const createSeaportDutchAuction = async ({}) => {
  return;
};
